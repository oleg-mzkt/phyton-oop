#28. Введение в обработку исключений. Блоки try / except | ООП Python
# Типы ошибок:
# - исключения во время исполнения (обработка исключений логики программы)
# - исключения при компиляции/интерпритации (нужно правильно писать программу без синтаксических ошибок)

try:
    f = open("myfile2.txt")
except FileNotFoundError:
    print("Невозможно открыть файл") # выведет, если файл не найден и продолжит выполнение программы

try:
    x,y = map(int, input().split())
    print(type(x),type(y))
    devision = x/y
except ValueError:
    print("Ошибка типа данных") # выведет, если файл не найден 
except ZeroDivisionError:
    print("Деление на ноль невозможно") 
    
print ("\nпродолжение выполнение программы",)
